<p style="color: green"><%= notice %></p>

<%= render @wishlist %>

<div>
  # <%= link_to "Edit this wishlist", edit_user_wishlist_path(user_id: @wishlist.user) %> |
  # <%= link_to "Back to wishlists", wishlists_path %>

  <%= button_to "Destroy this wishlist", @wishlist, method: :delete %>
</div>
